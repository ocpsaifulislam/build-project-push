# build-project-push

### Git Initialization
git init  
git add .  
git commit -m "Init"

### GitHub Push
git remote add origin https://github.com/ocpsaifulislam/build-project-push.git
git branch -M main  
git push -u origin main

### Create New Controller add api
 

### Second Commit
git add .  
git commit -m "Create Test Controller" 
git push

### add new Api

### third Commit
git add .  
git commit -m "get api test create"
git push
  